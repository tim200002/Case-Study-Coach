import { CaseSession } from "~/server/db/schema";
import { Parser } from "./statemachine/parser";
import { CaseStructureComponent } from "./statemachine/case_structure_component";
import { Statemachine } from "./statemachine/statemachine";

// class Chatbot {
//   stateMachine: Statemachine;

//   constructor(session: CaseSession) {
//     this.stateMachine = new Statemachine(session);
//   }
// }
